//Create a web server that can respond to requests for /comments.json with a JSON representation of the comments list and respond to requests for /comments/new with a form for submitting new comments.

// Use the comments variable defined below as your data. You will need to create only two types of pages: the main comments page and the submission form page.

// You can use jQuery on the client side to render the comments list and submission form to HTML. No need to do any HTML templating. We'll do templating in a later assignment.

// Don't spend much time styling your pages. We just want to see that you can respond to requests for JSON and HTML.

// var comments = [
    